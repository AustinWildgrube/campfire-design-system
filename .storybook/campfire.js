import { create } from '@storybook/theming';

import logo from '../stories/assets/logo.png';

export default create({
  base: 'light',
  brandTitle: 'Campfire Design System',
  brandUrl: 'https://ui.wldgrb.com',
  brandImage: logo,
});
